#include "monty.h"

/**
 * nop - perform nathing
 * @stack: the stack
 * @line_number: from where we call the function
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
